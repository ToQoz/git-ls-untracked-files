# git-ls-untracked-files

## Usage

```
$ git ls-untracked-files -h
usage: git ls-untracked-files [<directory>]
```

## Tips

`git ls-untracked-files` is too long. Let's define alias as you like.

```
# ~/.gitconfig
[alias]
	ut="ls-untracked-files"
```
